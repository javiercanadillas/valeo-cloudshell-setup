#!/bin/false
#shellcheck shell=bash

# custom_config.bash:   This script is intended to be sourced, it can be run in
#                       the event the Cloud Shell VM stops after a period
#                       of inactivity, and it will restore the necessary
#                       installed software and environment variables to its desired
#                       state
